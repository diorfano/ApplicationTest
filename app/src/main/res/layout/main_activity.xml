<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/BackgroundStyle"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.androidtest.MainActivity">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_recyclerList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/et_searchInput"
        android:scrollbars="vertical"></android.support.v7.widget.RecyclerView>

    <EditText
        android:id="@+id/et_searchInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_toLeftOf="@+id/ib_searchButton"
        android:layout_toStartOf="@+id/ib_searchButton"
        android:imeOptions="actionSearch"
        android:inputType="text"
        style="@style/TextStyle"
        >

        <requestFocus />
    </EditText>

    <ImageButton
        android:id="@+id/ib_searchButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/et_searchInput"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:adjustViewBounds="true"
        android:background="@drawable/view_clicked_states"
        android:src="@mipmap/ic_search_black_24dp" />


</RelativeLayout>